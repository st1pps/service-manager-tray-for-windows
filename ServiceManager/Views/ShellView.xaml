<controls:MetroWindow x:Class="ServiceManager.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiceManager.Views"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        Title="{Binding DisplayName}" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="35" />
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal">
            <Button Content="Refresh" x:Name="RefreshServices" />
            <Button Content="Open Service Mgr" x:Name="OpenWindowsServiceManager" />
            <Button Content="Open Task Mgr" x:Name="OpenTaskManager"></Button>
            <Button Content="Exit" x:Name="Exit"></Button>
        </StackPanel>
    </Grid>
</controls:MetroWindow>

